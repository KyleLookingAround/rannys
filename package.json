{
  "name": "rannys",
  "version": "1.0.0",
  "description": "Ranny's — coffee shop website. Static site built from YAML content.",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "serve": "node build.mjs && npx --yes serve dist -l 5173",
    "dev": "npm run serve"
  },
  "dependencies": {
    "js-yaml": "^4.1.0"
  },
  "devDependencies": {
    "esbuild": "^0.28.1"
  }
}
